test_in_portion: 0.9  # used if we want to test on synthetic data
num_test: 5000
feature_truncation: 'subsampling'  # 'subsampling', 'projection'
seed: 0 # 1, 2, 3, 4
inf_len: 5000
preprocess_transform: null
extra_suffix: ''
model_path_updated: null